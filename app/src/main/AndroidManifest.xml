<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.xzchang.food2fork"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".app.App"
        android:theme="@style/AppTheme">
        <activity android:name=".view.RecipieListActivity" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <meta-data android:name="android.app.searchable" android:resource="@xml/recipie_searchable"/>
        </activity>

        <activity android:name=".view.RecipieDetailActivity" android:screenOrientation="portrait" />
    </application>

</manifest>